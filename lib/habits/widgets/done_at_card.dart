import 'package:flutter/material.dart';
import 'package:lifehq/habits/models/done_at.dart';

class DoneAtCard extends StatelessWidget {
  const DoneAtCard({
    Key? key,
    required this.doneAt,
  }) : super(key: key);

  final DoneAt doneAt;

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
